import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'oiler-capacitor-ble-sdk-demo-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
